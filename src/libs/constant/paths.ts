/* eslint-disable no-unused-vars */

// Paths
export enum Paths {
  index = '/',
  account = '/me',
  admin = '/admin',
  authentication = '/auth',
  register = '/register',
  login = '/login',
  tokenRefresh = '/refresh',
  entries = '/entries',
  groups = '/groups',
  parts = '/parts',
  roles = '/roles',
  profiles = '/profiles',
  users = '/users',
  oauth = '/oauth',
  token = '/token',
  authorize = '/authorize',
  scopes = '/scopes',
}
